<%= form_for(@noise) do |f| %>
  <% if @noise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@noise.errors.count, "error") %> prohibited this noise from being saved:</h2>

      <ul>
      <% @noise.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :coordinates_longitude %>
    <%= f.text_field :coordinates_longitude %>
  </div>
  <div class="field">
    <%= f.label :coordinates_latitude %>
    <%= f.text_field :coordinates_latitude %>
  </div>
  <div class="actions">
    <%= f.submit :class => "btn" %>
  </div>
<% end %>
