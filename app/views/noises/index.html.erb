<div class="row">
  <%= render '/noises/search_form' %>
</div>
<div class="row">
  <%= link_to root_path, :class => 'hidden btn btn-large btn-block', :title => "See nearby only", :data => { :geolocatable => true} do %>
    <i class="icon-map-marker"></i> Nearby
  <% end %>
</div>

<%= render 'list' %>